# microservice-ecommerce-domain
In order to have a maven project with all domain objects, this project is generating all common classes and centralizing it, so that other projects can use it as dependency.
