# microservice-ecommerce-domain

In order to have all domain objects in a central place, this project is generating and holding all Entity, DTO and
Mapper classes, so that other projects can use it as single source of truth dependency.
